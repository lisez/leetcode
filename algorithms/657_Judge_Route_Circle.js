/**
 * @param {string} moves
 * @return {boolean}
 */
const judgeCircle = (moves) => {
  const x = {
    R: 1,
    L: -1,
  };
  const y = {
    U: 1,
    D: -1,
  };

  const getPos = (steps) => {
  };
};
